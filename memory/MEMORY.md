# Memory index — AsteroidDefense

- [Project overview](project-overview.md) — what it is, spec in HANDOFF.md, BNCL-1.0, public repo; MVP §10 tasks 1–10 all DONE (propagator → b-plane → clock → close-approach → deflection curve → egui viewer); the B1 gate that proved the 1/lead falloff is real.
- [Always commit and push](always-commit-push.md) — standing order: commit AND push every batch, don't wait to be told.
- [Git workflow](git-workflow.md) — update memory + docs, then commit & push at the end of every batch / "session end".
- [Kernel resolver](kernel-resolver.md) — **run tests as `ASTEROID_REQUIRE_KERNELS=1 cargo test --workspace --release`**; without it ~half the physics suite silently skips and still prints green. Kernels live at `W:\temp\claude\AsteroidDefense\kernels`, and **both resolvers reach them by a relative walk up from the repo** — moving the repo or the scratch root again breaks it silently.
- [Tier-2 forces](tier2-forces.md) — the force-model ladder, now CLOSED: 1PN + Yarkovsky + 16 sb441 perturbers + SRP + J2 + Pluto, each validated in isolation and measured as a b-plane shift (GR 55.6 / SRP 8.36 / J2 1.33 / belt 0.55 / Pluto 0.0006 km); the Apophis capstone vs Horizons; the hardcoded sb441 GM table and why; the live `[P]` force menu; J2's validity-boundary pair.
- [Mission porkchop](mission-porkchop.md) — Phase-2 §8 deliverability: Lambert (+ multi-rev, and the lapping-transfer bug it exposed), real launch-vehicle C3→payload tables, the `[4]` launch-window map and the cheap-departure bug it caught, `[M]` required impactor mass. Headline: one real launcher delivers ~1/65th of what the curve wants.
- [Deflection spectrum](deflection-spectrum.md) — §5 method spectrum **COMPLETE 2026-07-27**: nuclear = **wrong tool** (disruption regime), gravity tractor = **right tool at the wrong scale** (12.6× short, and it moves perigee 188 km the *wrong* way). Plus the **`[K]` tractor bench** (2026-07-27): six live knobs, why `a·T` is a lie as a live margin (+21%) and `a·T·(1−T/2L)` ships instead (−16%, errs safe), the 1/lead law's measured 1-orbit floor, and **the wall is `1/cos(plume)` = 1.064 radii, not the surface**. The rock it tugs is now dialable too — see [Threat orbit designer](threat-orbit.md).
- [Threat orbit designer](threat-orbit.md) — `[N]` puts the rock on a **different heliocentric orbit** (2026-07-28): a free closed-form preview (encounter geometry *exact*, orbit 0.23%), the two walls (offset ceiling is **exactly R⊕**, and shrinking it is what breaks the flyby), the gated rebuild, and the **per-orbit required-Δv anchor that must never fall back on the shipping constant** — nor be rescaled from it (off by 2.9×). Headline: the same plan scores **0.372× vs 1.096×** on two orbits.
- [gdext binding](gdext-binding.md) — the GDExtension core binding and the whole Godot physics frontend (real DE440 orrery, real threat, b-plane view, comet, sb441 bodies, Horizons NEOs); the verdict-bar bug (`b` vs `b_cap`); the kernel-skip trap; `_shot.gd` as the only honest visual check; measure-first as the recurring method. **Read the three staleness traps before any frontend work: Godot loads `target/debug/` (build BOTH profiles), a new `class_name` needs an editor rescan, and `mission_ready` now fires more than once so its handlers must be idempotent.**
- [Godot visual layer](godot-visual-layer.md) — the retro-CRT phosphor UI, code-built scene, 3D/2D views; **2026-09-05: world in its own MSAA viewport + peak-hold phosphor persistence, per-frame position memo, cached 2D traces (map 18.9→7.1 ms), `_perf.gd` + `run_harness.ps1` (run `-Headless`; kill only your own Godot PID — lingering Godot holds the debug DLL), keyhole map `[H]` finally seen and its captions budgeted;** GDScript now owns **zero** orbital mechanics (every drawn body names a source, unknowns `push_error` instead of returning ZERO). Three bottom-centre panels (planner `[M]`, tractor `[K]`, threat `[N]`) share an origin and the arrow keys, so they are mutually exclusive through one helper. **2026-09-06 frontend speed: startup wait 34.0 -> 25.2 s** (the comet flies on its own worker after install, not on the build worker) and **one batched binding call for every DE440 body** (3D views 29 -> 6 calls/frame, filled on demand — an eager fill made porkchop pay 0 -> 1 and stretched the build wait to 56 s); plus three traps — a parse error in an autoload hangs a headless run forever, the gdext suite needs `--test-threads=4`, and `_ready()` blocks ~11 s in `load_from` on a cold cache. **2026-09-07 startup: the kernel read went onto its own worker (`_ready` 29 -> 2 ms) — and the roadmap's "646 MB DE440" was a 32 MB file on a fragmented spinning disk. Read trap (d) there before ANY frontend work: `bodies_online` is no longer true at scene load, and getting that wrong throws in `_process` every frame, which kills every line below it while both headless suites stay green.**
- [Godot Phase-2 scaffold](godot-phase2-scaffold.md) — `godot/` project root, Godot 4.7, gdai-mcp plugin gate; precision RESOLVED = SINGLE (Rust keeps f64 and hands Godot small residuals).
- [Encounter CA snap](encounter-ca-snap.md) — `[C]` in the b-plane view pauses and jumps to the live track's closest approach; argmin on the core's polyline, sampling floor ≠ error.
- [Godot MCP script cache](godot-mcp-script-cache.md) — game runs read `.gd` from disk; the editor resource cache goes stale (reload via `execute_editor_script`).
- [Keyhole reach](keyhole-reach.md) — keyholes **priced before building** (2026-07-28) and then **built and merged to main 2026-09-02**: closed-form `a'(b,B̂)` (so *don't* go looking for Valsecchi), the counter-intuitive headline that **the wide keyholes are the far ones**, and now `core/src/keyhole.rs` with the ξ,ζ frame pinned, the 3:4 flown to a **return impact for Δv 0.2166 m/s**, plus the repo's first CI. The Godot `[H]` overlay **ran and was seen 2026-09-05**; the unbounded circle radius is **closed 2026-09-07** — the core was right and the *drawing* was 59.9 px wrong, now clipped and budgeted in `plot_geometry.gd`.
- [Keyhole targeting](keyhole-targeting.md) — **2026-09-05**: aim/fly/refine as a core API (`core/src/keyhole_target.rs`) and the planner's `KEYHOLE` row. The return read in its **own** Öpik frame (ξ₂ 4 014 km spatial vs ζ₂ 891 km timing — the check that says a floor really is one); the keyhole width measured **1.6× conservative** against the door the rock flies through, so the panel prints *widths*, never a yes/no; and the coarse-sweep trap that nearly concluded keyholes were unreachable from the frontend (they are: 0.930 m/s at 900 d → 2.9 widths off 3:4 under a `MISS - EARTH CLEAR` verdict). **2026-09-06: the second resonance (7:9) broke three constants** — both census gates were only ever right for the 3:4, and the ξ₂/ζ₂ gauge caught the probe calling a number a physical floor when the search had converged onto the **upper wall of its own bracket** — the widening step was constant instead of doubling, and a vanishing Δv window makes a wall look like a tight answer. **2026-09-06 (item 6): the floor was a stopping distance** — the 891-vs-786 km contradiction was two shots 1.9e-7 m/s apart (never round a keyhole Δv: ζ₂ moves 5.4e8 km per m/s), neither was the minimum (20 iterations gives Δv 0.2165483096 → 1 087 km, ζ₂ −26.6 km — the timing residual really does go to zero), and `dv_window_m_s` is the **iteration budget**, not a door width.
- [Keyhole placement](keyhole-placement.md) — **roadmap item 4 CLOSED 2026-09-07 on five flown keyholes**: the linearised width is sound (conservative by ≤1.44× once the return's own offset is divided out, proved by b = 11 240 ± 60 km at all ten door edges, not fitted), but **four of the five doors do not contain their own circle** — placement is 1.0 to 27.8 half-widths off and obeys none of the three candidate laws, so the frontend's 4.0-half-width alert is replaced by an additive 100 km band. Plus the `is_impact_return` capture-radius bug this caught.
- [Keyhole lead sweep](keyhole-lead-sweep.md) - **2026-09-07, item 4 CLOSED**: the placement band was calibrated at a lead the planner **cannot dial** (12 yr vs a 900 d cap); the same 3:4 door sits +19.3 km from its circle there and **+210.6 / +467.9 km / no door at all** at 900 / 450 / 150 d, so the constant went 100 -> 500 km. The knob is **lead time**, not a sideways impulse (2-D reachable patch, no v_inf confound). Three traps: a circle is crossed **twice**, tightest-pair-anywhere is not spacing-near-this-plan, and a ladder belongs to one lead.
- [Keyhole lead is the variable](keyhole-lead-variable.md) - **2026-09-07**: of lead, dv, xi and angle-round-the-circle, **only the lead** orders all five flown doors (the 200 d door takes a *smaller* nudge and sits *further* out; the 300 d and 12 yr doors are 0.48 deg of arc apart with 34x the error); rebuilding each flight's own Opik frame makes it **worse** (767 -> 1375 km) though the 1.9 h arrival slip alone moves a circle ~200 km; band 500 -> **800**, width claim 1.44x -> **2.11x** on a closing door; and the crowded register **fires at every dialable lead** once you gate on the pass actually being a miss.
- [Keyhole ranking](keyhole-ranking.md) - **2026-09-07**: the core ranked circles by keyhole *widths*, which divides away the additive placement error the five-door batch measured; replaced by `margin` (km outside a door), with the alert and the label now cut on the same row. Measured **0 disagreements in 2 000 geometries** (runner-up within ~0.1 km), so it is the principled key and not a caught bug - plus the +/-3-capture-radii sampling box that meant the sweep had never asked, and the note branch nobody had seen fire, now executed.
- [Keyhole domain floor](keyhole-domain-floor.md) - **2026-09-07**: below 200 d there is no door for the placement band to be wrong about (three circles floor 19-29 thousand km out at 125 d with their timing spent; the 3:4 circle is unreachable by 100 d), so the constant stays 800 and gains a stated domain floor - plus the three gates that had to be discarded or fixed to make a negative mean anything, and the 200 d control that reproduces a known door to three decimals.
- [Keyhole probability](keyhole-probability.md) — **2026-09-06**: the covariance flown through *both* encounters (chaining in the propagation, not a matrix product); the 778x flyby gain checked against the closed form to 14%; and the headline that at a keyhole **P is set by how well the orbit is KNOWN, not how well the impulse is aimed** (flat 0.064 across the whole door until the whole covariance shrinks). Three more numbers caught asserting a claim instead of measuring one, including a doc its own probe had been contradicting since July.
- [SBDB covariances](sbdb-covariance.md) — **2026-09-06**: real JPL orbit uncertainty ingested (cometary elements, 8x8 with the non-grav terms marginalised, its own epoch); why a round-trip could not validate it and the three external gates that do; the 9.8° cigar tilt that is a 14× correlation cancellation and not a bug; the sphere-of-influence trap that faked a 4 125 km dynamical error; and the headline that Apophis' **real** 2029 ellipse (18.2 km, P = 0) is the **same size as our own dynamical error** (15.1 km). One obliquity constant now, in `core/src/frames.rs`.
- [Integrator convergence](integrator-convergence.md) — **roadmap item 5 retired 2026-09-06 by measurement, not by building IAS15**: dop853 IS converged where it ships (the keyhole timing coordinate moves 365 m across four decades of tolerance, 1.5% of a keyhole width — read the deltas, not the rounded absolute) — but only because the **1-day snapshot cadence caps the step**; the same tolerance uncapped is 129 km off over the cruise. The cadence-x-tolerance cross (40 000x collapse), the flyby’s measured 5 558x amplification that closes on itself, why the 118 m Tier-3 bias is not in the derivative, and why IAS15 has **no oracle** to prove anything against.
- [Tier-3 ellipse on the b-plane view](tier3-uncertainty-view.md) — `[U]` draws the orbit's 1σ spread, `[Z]`/`[X]` re-ask it at a better- or worse-known orbit (13 propagations held, so the knob is free). The frame trap: every scalar is invariant under the sensitivity's arbitrary basis but **an ellipse's orientation is not**, so the rotation into the view's axes happens in the core (residual 1.95e-10). Result: **168.71 × 0.82 km lying 0.3° off ζ̂**, the timing axis. **2026-09-07: its drawn *shape* now survives the ±3σ shell** — but the linearity scalar that was meant to say so is blind to the minor axis (0.0043 vs 0.561, a factor of 130), and the residual is a noise floor plus curvature where only one is physics. **2026-09-08: that number moved into the module** (`LinearityReport::shape_residual`, out of two private copies) and the 130x turned out **not** to be the 205:1 aspect ratio - `shell_scale` is 0.693 of the 3sigma half-length, so it is 143 half-widths of shell reach x 0.90 of the residual lying across the needle. The move also **flipped an eigenvector sign nothing tested** (drawn angle -90.26 vs a published 89.736, every ratio unchanged) - pinned now, and the print folds into a half-turn.
- [Tier-3 uncertainty](tier3-uncertainty.md) — the covariance→b-plane→impact-probability layer (2026-07-28): the **quantised-argmin trap** that makes a plausible-but-wrong Jacobian, the **pinned 10-day sample cadence** and why it is not a caller's knob, whitened-coordinate impact probability (polar-on-the-disc returns 0.994 for an answer of 1), and `sigma_distance` reading 8 196 σ *with* P = 1. What the keyhole batch still owes.
- [Keyhole prediction vs condition](keyhole-prediction-vs-condition.md) — **2026-09-08**: the placement error is in the map's **prediction** of the post-flyby orbit, not in the resonance **condition** — a flown door leaves the rock 14–49 km-equivalent from `a_res` at every lead (−14 to −16 km at three of four, −49 at 200 d, inside a ±41 km bar — and a 30 d shift of the averaging window moves every row the same −21 km, so the outlier is real) while the closed form's `a'` is off by 33 → 839 km, which *is* the ladder. Both cheap repairs measured and dead, including the `r ≈ R⊕` one the module doc had blamed since it was written (0.4 % at the 300 d door). Plus the revolution-mean observable that had to be built first.
- [Keyhole baseline, not turn](keyhole-baseline-not-turn.md) - **2026-09-08**: the placement ladder is in the map's **baseline** (which orbit the deflected rock arrives on), not in the turn - the two absolute errors run 750 and 805 km across the extreme doors while the error in the *change* across the encounter is **55 km**, inside a +/-136 km bar. All four inputs swapped one at a time are innocent and additive; the long-blamed r-approximation is real but lives on the *incoming* leg where it cancels. Drawing the circle on the change collapses a **767 km ladder** - measured here, shipped in [Keyhole circle on the change](keyhole-circle-on-change.md).
- [Keyhole circle on the change](keyhole-circle-on-change.md) — **2026-09-08, the repair SHIPPED**: the circle is now placed on the **change** the flyby makes (`resonant_circle_on_change`, fed by the arriving orbit read off the flown arc at 10 Hill radii), collapsing the 767 km lead-ladder to **20.7 km**. And the gate — a 2:3 flown on a *crossing* Δv, no door needed — says the leftover error is a constant of **semi-major axis**, not of b-plane km (12× apart in km = the gradient ratio; overlapping in `a'`; the same ±3 5xx km bar on both). So `KEYHOLE_PLACEMENT_KM = 800` → **`KEYHOLE_PLACEMENT_A_KM = 15 000`** converted per circle (575 km on a 3:4, 57 km on a 2:3), and `exposure = margin − band` is the alert cut *and* the ranking key.
- [Keyhole constant not one number](keyhole-constant-not-one-number.md) — **2026-09-23, item 9 CLOSED**: 21 flights / 8 resonances, one sign but 2 831..46 806 km of `a'` (close orbit-raising 6:5 & 7:6 are 2-5x), so the band went **UP 15 000 -> 51 000** (user chose blanket raise; 3:4 zone ~575 -> ~1 950 km); crowded register NOT reachable on shipped circles (the old firing was the km-unit bug in `stage_crowding`).
