# Memory index — AsteroidDefense

- [Project overview](project-overview.md) — what it is, spec in HANDOFF.md, BNCL-1.0, public repo, current phase (§10 tasks 1–6 DONE + task 7 DONE: RK4/dop853/ANISE-field/ASSIST-validation ~4.5e-11 + task 8 DONE: b-plane geometry.rs hit test + task 9 DONE §10.9: dop853 dense output + fixed-cadence clock.rs, sub-snapshot from dense output not linear interp, D-matrix pinned by interior-point test + close-approach detector DONE: close_approach.rs range-rate root-find on Clock segments, Earth velocity surfaced (state_km_s/EphemerisPerturber::state_at/GeocentricState), CloseApproach::b_plane closes the pipeline, max_sample_dt required cap vs silent aliasing + task-10 Commit A DONE: core/src/deflection.rs — apply_impulse/along_track/kinetic β + DeflectionScenario.required_dv solves headline Δv-vs-lead curve by bracket+bisect on b-plane PERIGEE (not raw CA), NotHyperbolic→hit, massless-Earth needs fast v_rel, 7 kernel-free tests incl. sub-orbital thesis; kernel located at temp/AsteroidDefense/kernels (de440s.bsp+pck11.pca); OPEN: Pluto-in-shipping deferred to Tier-2, b-vector sign/ξζ deferred to Tier-3 + task-10 Commit B DONE (ef82cfa §10.10): egui viewer — B1 GATE PASS real-field log-log slope −1.049/−0.912 → 1/lead falloff REAL; deflected_trajectory core add (Clock+encounter, evaluate delegates); scenario.rs designer DE440 impactor (a=0.855AU T=0.79yr) + CurveFile/curve.json cache (gitignored, computed once) + EncounterFrame/frame_from geocentric tracks reusing prebuilt ds (12-yr nominal scan hoisted, 401s DEBUG artifact); egui app worker-thread nudge + curve panel + encounter painter (nominal spears / deflected slides, play sweeps in-view band); eframe 0.35 App::ui/Panel churn, egui_plot 0.36↔egui 0.35; GUI compiles+physics-tested but UNRUN (no display) — needs manual `cargo run -p viewer --release`, play animation esp.; carry: real-Earth focusing untested, 1.0-orbit phasing bump not a bug)
- [Kernel resolver](kernel-resolver.md) — run Rust tests as `ASTEROID_REQUIRE_KERNELS=1 cargo test --workspace --release`; core/src/kernels.rs (env→dirs, both-or-nothing) + the flag that makes a skipped suite FAIL instead of printing green; kernels live at temp\AsteroidDefense\kernels
- [Encounter CA snap](encounter-ca-snap.md) — [C] in the b-plane view pauses + jumps to the live track's closest approach; argmin on the core's polyline, sampling floor ≠ error
- [Git workflow](git-workflow.md) — update memory+docs, commit & push at end of every batch / "session end"
- [Godot MCP script cache](godot-mcp-script-cache.md) — game runs read .gd from disk; editor resource cache goes stale (reload via execute_editor_script); camera-default edits are overridden by focus targets
- [Godot Phase-2 scaffold](godot-phase2-scaffold.md) — godot/ project root (addons moved in), Godot 4.7; gdai-mcp plugin load GATE=PASS (binds :3571); precision RESOLVED = SINGLE (advisor-gated: Godot is a viewer, physics stays Rust/f64, Rust rebases to view-focus origin in f64 & hands Godot small f32 residuals → no jitter even for far/early deflection; double would need from-source build, rejected); gdai-godot MCP REGISTERED local scope (`uv run` proxy → :3571), go-live = open editor + `/mcp` reconnect; gdext binding of core/ (default single) still ahead
- [Godot visual layer](godot-visual-layer.md) — retro-CRT phosphor UI (green/amber, native-res, style-only) DONE & screenshot-verified: code-built scene (main.gd assembles SubViewport+CRT shader; SubViewport-child Controls need explicit sizing), 3D vector solar system (glow_line instance uniforms, wire spheres/blobs, comet GPUParticles tail), 2D radar map, HUD/tags/boot, Sim autoload display-grade Kepler (threat built FROM impact condition, deflection Δa/a=2e-3 emergent divergence), InputMap actions drivable by simulate_input; editor ProjectSettings.save() clobbers manual project.godot edits — use editor-script API; the encounter/b-plane close-up section (key 3) is **SUPERSEDED 2026-07-17 by Phase-2 3C-2c** — that view was display-grade and its self-computed numbers were wrong (v_inf 3.17 from CA speed vs real hyperbolic excess 7.63 → capture circle 3.7 R⊕ vs true focused 1.773 R⊕); the f64 helpers are deleted and encounter.gd now reads the core's EncounterFrame (see [[gdext-binding]]) — **the display-grade Kepler is now ENTIRELY GONE (2026-07-20, Phase-2 3D)**: the threat went real in 3C-2b and the comet in 3D, and with the comet went `_elements`/`_kepler_pos_ecl`/`solve_kepler` and the `pos_ecl`/`orbit_points` Kepler fallbacks. GDScript now owns zero orbital mechanics; every drawn body names a source (`ephem`/`threat`/`threat_defl`/`catalog`) and an unknown one `push_error`s rather than returning ZERO. The comet's GPUParticles tail and blob survive — only the physics behind its position changed. See [[gdext-binding]].
- [gdext binding](gdext-binding.md) — Phase-2 GDExtension core binding: godot/rust/=asteroid_gdext cdylib workspace member → core; Commit-1 toolchain gate PASS (API4.6 loads in runtime4.7, core_version round-trip); editor-tool-context = placeholder instances so verify in game runtime (headless test_gdext.gd); reloadable=true solves the Windows DLL rebuild lock; 2A–2C+3A/3B/3C-1/3C-2a DONE (real DE440 orrery on a real TDB clock); **3C-2b DONE — the threat is REAL and on screen** (mission_online true; ast_el = the core's integrated trajectory; planner → Mission.set_plan → b-plane perigee; GDScript owns zero orbital mechanics, the f64 Kepler block + test_sim.gd DELETED): measuring first re-sliced it — set_plan's 11 s was a DEFECT (deflection() re-flew the 12-yr nominal per nudge) → OnceLock nominal cache = 0.7 s + build halved to 10 s; Send+Sync on the 3 core traits (Send ALONE fails — ForceModel is decorated, &T:Send needs T:Sync); profile-dev opt-level=3 on asteroid_core makes the debug DLL Godot loads 10.7 s not 400 s; build runs on a worker (Arc<Ephemeris> out, BuiltScenario back — the serving core must NOT move or the planets freeze); capture_radius_m() = **11 311 km = 1.773 R⊕** (v_rel=18 is at the 3000 km impact point, v_inf is only 7.63 km/s) is the honest verdict bar; **the gate is 4 flags not 1** (comet/interceptor/encounter stay dormant on their own — one flag would light 3 lies); **threat_span_tdb() + threat_active()** = the 2nd ZERO-is-the-Sun trap (clock clamps to the ~300 yr KERNEL, threat exists ~12 yr); **clean miss returns perigee -1 — the BEST outcome shares the no-plan sentinel**, so verdict = is_clean_miss() or perigee>cap, centralized in miss_label/verdict_label; user chose DEBOUNCE (0.35 s) for the solve hitch; _draw() DOES run headless BUT only for VISIBLE nodes (planner/map2d never ran) → **verify visually with the _shot.gd autoload**, which showed the threat live, the planner at 0.32 LD / EARTH CLEAR, and — the money shot — the threat GONE (not on the Sun) when scrubbed to 2149; GDScript resolves autoload methods at RUNTIME (not parse time) — load the project, `--check-only --script` can't see autoloads; **3C-2c DONE — the b-plane view is real AND it caught the verdict being wrong**: `perigee > capture_radius` is NEITHER coherent pair (b pairs with b_cap, perigee pairs with R⊕ — equivalent only as pairs) → ~1.5× too strict, printed SURFACE IMPACT over a reachable plan (0.2 m/s @ 1 period: b=14 639 clears the 11 311 disc, perigee=9 319 sits inside); user chose b-vs-b_cap, `miss_ld` is now b since PROJ MISS prints above CAPTURE; core `frame_from_arcs`+`DeflectedArc` (no 2nd propagation, pair travels as one value), projection ALL-ICRF in the binding (`ecliptic_north_icrf()`, NOT (0,0,1)), `_shot.gd` KEPT — `_draw()` only runs headless for VISIBLE nodes; **THE KERNEL-SKIP TRAP** — `cargo test` without `ASTEROID_DE_KERNEL`/`ASTEROID_PLANETARY_CONSTANTS` exported SILENTLY SKIPS ~half the physics suite and prints "13 passed" (the `eprintln!` guard is swallowed — cargo releases stderr only for FAILING tests); **runtime is the only tell: 0.02 s vs 69 s** — it made both of this batch's verification claims vacuous (the rescale "confirmed" by a test that never ran; `frame_from_arcs_matches_frame_from` never once executed) though both re-ran green; machine HAD the kernels, only env was unset; GDScript immune via `Kernels.resolve()` env→cfg→dirs; Rust-side resolver NOW BUILT 2026-07-19 — see [[kernel-resolver]]; prove a gate has teeth by bypassing the fix and watching it FAIL; deflected b-point RESCALED to its own |B| (own b-plane ⊥ own Ŝ; raw projection plots √(|B|²−s²) inside the disc while the panel says MISS; gap 0.00% → guarantee not fix); live marker = radar contact NOT a diamond (sharing the glyph made the legend's "MARKS = ASYMPTOTE" a lie — the verdict bug in miniature), reached by the [C] key since 2026-07-19 (was: commit+scrub into ±1.5 d AND `Sim.paused=true`, warp overshoots CA by 0.53 d → off-plot) — see [[encounter-ca-snap]], shows perigee 0.024 LD INSIDE b-point 0.038 LD + **3D comet-half DONE 2026-07-20**: scope SPLIT (advisor, user agreed) — real NEOs are a DIFFERENT design (sb441-n16.bsp already CONTAINS the ephemeris → read it like the planets' "ephem" path, NOT add_synthetic_body which integrates elements); measure-first again decided placement (comet 2.0 s/12 yr, 8.1 s/45 yr vs build 11.2 s → folds into the EXISTING build worker, not inline): free fn seed_orrery_body so worker + add_synthetic_body cannot drift, BuiltScenario::scenario_ref()/epoch0(), install(built, bodies), span = one orbit 22.6 yr; display_comet authored by hand-converted true anomaly (elements.rs has NO mean-anomaly API) ν₀=176.8° → measured perihelion 0.807 AU / +0.97 yr from impact, re-measured by the test so a careless seed edit fails loudly; **GDScript's LAST Kepler deleted** (comet was the only user of _elements/_kepler_pos_ecl/solve_kepler + the pos_ecl/orbit_points fallbacks) and the fallback now push_errors instead of returning ZERO; **ZERO-is-the-Sun 3rd instance, first caught BEFORE shipping** — per-body Sim.catalog_active() off catalog_span_tdb, comet_online set from what the catalog HOLDS not alongside mission_online, test_orrery's old "not comet_online" assertion INVERTED +8 checks; proof is two _shot.gd pictures (on-arc 4.4 AU tagged; 2051 comet GONE not on the Sun) + **sb441 half DONE** (16 main-belt PERTURBERS mounted, NOT a target list — no Apophis in that file) + **Horizons NEO half DONE 2026-07-20**: the recorded "reuse the identical read path" was FALSE — Horizons per-object SPKs are **type 21** and ANISE 0.10.3 can't evaluate it (proved by `probe_horizons.rs` BEFORE any plumbing); advisor-pivot to **VECTORS→sampled `.neo` state table + cubic Hermite** in new `core/src/horizons.rs` (interpolates JPL's own relativistic states, NOT our integration → dodges the Tier-2 1PN trap; NEOs are SCENERY, never threat/target); Apophis=NAIF **20099942** (`20000000+n`, NOT sb441's `2000000+n`); accuracy MEASURED (median 24 m, worst 18 885 km at the 2029 flyby = sub-pixel) vs a committed hourly fixture that runs kernel-free; `OrreryBody::Trajectory::{Integrated=our-physics/SSB, Sampled=JPL/helio-ICRF}` reconciled in ONE `catalog_body_helio_ecl_au`, `catalog_provenance` labels it; **ZERO-is-the-Sun 5th** (tables span 2020–2070, per-body gate, `catalog_active` no longer keys off the single `comet_online`); threat unchanged STRUCTURALLY (sampled NEO never reaches the almanac/GM — `==` test, cap 11311/|B| 14639 to the digit); orbit lines draw ONE lap via vis-viva period; the advisor's **co-location check** (Apophis on Earth at the real 2029 flyby, 0.0073 AU — the only non-`norm()` test, catches a Sampled-branch frame bug) — see [[gdext-binding]]; NEXT = Tier-2 validation vs Horizons, needs 1PN+Yarkovsky)
